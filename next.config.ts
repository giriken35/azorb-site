import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // API routes are handled natively by Next.js on Vercel.
  // No external backend proxy is needed.
};

export default nextConfig;
